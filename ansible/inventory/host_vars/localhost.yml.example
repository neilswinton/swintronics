---
# Host identity
server_admin_username: "your-name"    # OS user on this server
tz: "America/New_York"                # timezone (e.g. America/New_York, Europe/Berlin)
server_domain: "example.com"          # domain for this deployment

# Git identity
git_user_name: "Your Name"
git_user_email: "you@example.com"

# Data disk
data_disk_mountpoint: /docker-data
immich_data_location: "{{ data_disk_mountpoint }}/volumes/immich"

# Tailscale
tailscale_tag: "server"               # role prepends "tag:" automatically
tailscale_authkey_secret: "TS_MACHINENAME_AUTH_KEY"  # Infisical secret name
tailscale_subnet: "192.168.1.0/24"   # advertise-routes subnet (leave blank to skip)
tailscale_exit_node: false            # advertise as exit node

# DNS
dns_hostname: "machinename"           # <dns_hostname>.swintronics.com A/AAAA records

# TLS
cert_resolver: "production"           # or "staging" for Let's Encrypt staging

# Beszel agent — set after logging into hub and adding a system (TCP mode, port 45876)
# beszel_agent_key: "ssh-ed25519 ..."
# beszel_agent_token: "..."

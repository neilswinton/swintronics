# Managed by Ansible. Do not edit — changes will be overwritten on next deployment.

name: linkwarden
include:
  - ../networks.yml

services:
  postgres:
    image: postgres:16-alpine
    env_file: .env
    restart: unless-stopped
    volumes:
      - ./pgdata:/var/lib/postgresql/data
  linkwarden:
    env_file: .env
    environment:
      - DATABASE_URL=postgresql://postgres:${POSTGRES_PASSWORD}@postgres:5432/postgres
      - TZ=${TZ:-America/New_York}
    restart: unless-stopped
    image: ghcr.io/linkwarden/linkwarden:{{ versions.linkwarden }}
    ports:
      - 3000:3000
    volumes:
      - ./data:/data/data
    depends_on:
      - postgres
      - meilisearch
    labels:
      traefik.enable: true
      traefik.http.routers.linkwarden.entrypoints: websecure
      traefik.http.routers.linkwarden.rule: Host(`linkwarden.${SERVER_DOMAIN}`)
      traefik.http.routers.linkwarden.tls.certresolver: ${CERT_RESOLVER}
  meilisearch:
    image: getmeili/meilisearch:v1.12.8
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./meili_data:/meili_data
